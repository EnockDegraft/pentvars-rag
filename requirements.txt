# Core RAG pipeline
chromadb>=1.5.0
scikit-learn>=1.8.0
numpy>=2.0.0

# Backend API
fastapi>=0.141.0
uvicorn>=0.46.0
pydantic>=2.13.0

# Optional: enables the high-quality local embedding model instead of the
# automatic TF-IDF fallback. Safe to leave installed even if it's never
# used -- ingest.py only imports it inside a try/except.
sentence-transformers>=6.0.0
